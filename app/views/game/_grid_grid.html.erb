<%= grid(@tasks_grid,show_filters: :when_filtered) do |g|
  g.column name: 'Player/Team 1' ,attribute: 'party1' do |task|
    #@team.find(task.party1).nam
    printp(task.party1, @event.event_type.to_i)
    #@team.namefrfrf
    #task.party1
  end

  g.column name: 'Player/Team 2' ,attribute: 'party2' do |task|
    #Team.where(id:task.party2).parent_id
    #@team.name
    printp(task.party2, @event.event_type.to_i)
  end
  g.column name: 'Schedule', attribute: 'schedule' do |task|
    task.schedule
  end
  g.column name: 'Description', attribute: 'description' do |task|
    task.description
  end
  g.column name: 'Result', attribute: 'result' do |task|
    task.result
  end
end %>
